<Window x:Class="CourseSelectionSystem.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CourseSelectionSystem.ViewModels"
        mc:Ignorable="d"
        Title="学生选课系统 - 登录" Height="450" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="欢迎登录"
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,40,0,30"/>

        <StackPanel Grid.Row="1" Margin="40,0,40,0">

            <TextBlock Text="账号 / 工号" Margin="0,10,0,5"/>
            <TextBox Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5,0,0,0"/>

            <TextBlock Text="密码" Margin="0,15,0,5"/>
            <PasswordBox x:Name="pwdPassword" Height="30" Padding="5,0,0,0"/>

            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="Red" 
                       Margin="0,10,0,0"
                       Height="20"/>

            <Button Content="登 录" 
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=pwdPassword}"
                    Margin="0,20,0,10"
                    Height="40"
                    Background="#4CAF50" Foreground="White"
                    FontSize="16"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock Text="还没有账号？"/>
                <Button Content="立即注册" 
                        Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" 
                        Foreground="#2196F3"
                        FontWeight="Bold"
                        Command="{Binding OpenRegisterCommand}"
                        Padding="5,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>