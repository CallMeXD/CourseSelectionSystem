<Window x:Class="CourseSelectionSystem.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CourseSelectionSystem.ViewModels"
        mc:Ignorable="d"
        Title="新用户注册" Height="550" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="用户注册"
                   FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,10,0,20"/>

        <StackPanel Grid.Row="1">

            <TextBlock Text="账号 (学号/工号)" Margin="0,5,0,5"/>
            <TextBox Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5,0,0,0"/>

            <TextBlock Text="姓名" Margin="0,15,0,5"/>
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5,0,0,0"/>

            <TextBlock Text="选择角色" Margin="0,15,0,5"/>
            <ComboBox ItemsSource="{Binding Roles}" 
                      SelectedItem="{Binding SelectedRole}" 
                      Height="30" Padding="5,0,0,0"/>

            <TextBlock Text="设置密码" Margin="0,15,0,5"/>
            <PasswordBox x:Name="pwdPasswordReg" Height="30" Padding="5,0,0,0"/>

            <TextBlock Text="确认密码" Margin="0,15,0,5"/>
            <PasswordBox x:Name="pwdConfirmPasswordReg" Height="30" Padding="5,0,0,0"/>

            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="Red" 
                       Margin="0,10,0,0"
                       Height="20"/>

            <Button Content="注 册" 
                    Command="{Binding RegisterCommand}"
                    Margin="0,20,0,10"
                    Height="40"
                    Background="#2196F3" Foreground="White"
                    FontSize="16">
                <Button.CommandParameter>
                    <MultiBinding Converter="{StaticResource MultiValueConverter}">
                        <Binding ElementName="pwdPasswordReg"/>
                        <Binding ElementName="pwdConfirmPasswordReg"/>
                        <Binding RelativeSource="{RelativeSource FindAncestor, AncestorType={x:Type Window}}"/>
                    </MultiBinding>
                </Button.CommandParameter>
            </Button>

            <Button Content="返回登录" 
                    Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" 
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                    HorizontalAlignment="Center"
                    Padding="5,0"/>
        </StackPanel>
    </Grid>
</Window>